[project]
name = "game-mosta-v04-terminal"
version = "0.4.0"
description = "Red Monster — terminal (curses) version"
requires-python = ">=3.11"
dependencies = [
    "pillow>=10",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
